/**
 * Credit vs. debit for the month. A single stacked bar is enough — this does not
 * need its own screen.
 */
export default function MethodSplitBar({ month }: { month: string }) {
  // TODO: derive.methodTotals -> one stacked BarChart row.
  void month;
  return <section className="mt-6" />;
}
